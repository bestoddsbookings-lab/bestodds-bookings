services:
  - type: web
    name: bestoddsg-web
    env: docker
    branch: main
    dockerfilePath: ./Dockerfile
    plan: starter
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "5000"
